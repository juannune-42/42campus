[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
description = "Maze generator and solver library"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["mazegen"]
