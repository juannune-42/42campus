[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "mazegen"
version = "1.0.0"
description = "Reusable maze generator — recursive backtracker algorithm."
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["mazegen"]
